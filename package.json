{
  "name": "order-dashboard",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "classnames": "^2.2.6",
    "font-awesome": "^4.7.0",
    "node-sass": "^4.13.1",
    "object-path": "^0.11.4",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1",
    "request-promise": "^4.2.4"
  },
  "scripts": {
    "start": "yarn run start:dev",
    "start:dev": "yarn run start-api-server & react-scripts start",
    "start-api-server": "./node_modules/.bin/json-server --watch ./src/mocks/orders.json --port 13000 --host 0.0.0.0",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "json-server": "^0.15.0"
  }
}
